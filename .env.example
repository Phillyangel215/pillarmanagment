# Demo Mode (set to 0 for production, 1 for demo)
VITE_DEMO=1

# Base path for GitHub Pages
VITE_BASE=/pillarmanagment/

# Production backend (leave unset if using demo mode)
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# SMTP (production)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=

# Use email function proxy
VITE_USE_EMAIL_FN=1